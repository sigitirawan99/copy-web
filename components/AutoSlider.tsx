import ScrollVelocity from "./ScrollVelocity/ScrollVelocity";

const AutoSlider = () => {
  return (
    <div>
      <ScrollVelocity texts={["React Bits"]} className="custom-scroll-text" />
    </div>
  );
};

export default AutoSlider;
